@model WikiJuegos_PNT1.Models.Comentario
@using System.Security.Claims;

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Comentario</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" asp-controller="Comentarios">
            @Html.AntiForgeryToken()

            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="JuegoId" />
            <div class="form-group">
                <input asp-for="Texto" class="form-control" style="width: 100%" placeholder="Escriba su texto"/>
                <span asp-validation-for="Texto" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Puntaje" class="control-label"></label>
                <input asp-for="Puntaje" type="number" min="1" max="5" class="form-control" />
                <span asp-validation-for="Puntaje" class="text-danger"></span>
            </div>
            @{
                var userId = User.Claims.Where(c => c.Type == ClaimTypes.NameIdentifier).Select(c => c.Value).SingleOrDefault();
            }
            <hb>
            <div class="form-group">
                <input type="submit" value="Enviar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
